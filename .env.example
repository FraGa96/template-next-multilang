NEXT_PUBLIC_API_BASE_URL=   # backend API base URL, no trailing slash
NEXT_PUBLIC_SITE_URL=       # canonical base URL of this app, no trailing slash (used by robots.ts/sitemap.ts)
API_INTERNAL_URL=           # optional: server-side/SSR base URL, e.g. Docker-internal service name
